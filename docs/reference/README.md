# Reference Docs

This directory contains useful but non-default documents.

Use these files when a task needs:

- historical OV boundary rationale
- retrieval semantics details
- legacy pack examples

Do not treat this directory as the default source of truth for day-to-day development.
